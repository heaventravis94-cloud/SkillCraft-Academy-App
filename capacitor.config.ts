import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.skillcraftacademyapp.app',
  appName: 'SkillCraft Academy',
  webDir: 'dist'
};

export default config;
